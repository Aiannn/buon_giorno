# 🇮🇹 Проект Buon Giorno: Техническая Документация (MVP)

Данная документация описывает текущую архитектуру игры, систему процедурной генерации ландшафта и физику главного героя.

---

## 🏔 1. Система Procedural Terrain

Твой террайн — это **гибридная система**, сочетающая детерминированный дизайн (шаблоны) и математическую плавность (сплайны).

### Архитектура и Механизмы

- **Чанковая система (Chunk-based):** Вместо чистого случайного шума мир строится из предопределенных «блоков» (`ChunkTemplate`). Это дает полный контроль над геймдизайном: категории `Easy`, `Medium` и `Hard` позволяют точно настраивать кривую сложности.
- **Сшивание (Stitching):** Происходит через переменную `lastCursor`. Каждый новый чанк смещается по осям X и Y относительно конца предыдущего. Это гарантирует отсутствие «дыр» и бесконечную масштабируемость геометрии.
- **Гардрейл (Safe Guardrail):** Внедрена система фильтрации точек в методе `safeAdd`. Если расстояние между двумя соседними вершинами меньше $0.1$ единиц, точка игнорируется. Это критически важно для **Forge2D**, так как физический движок выдает ошибку, если вершины `ChainShape` находятся слишком близко.
- **Интерполяция Catmull-Rom:** Алгоритм превращает «ломаные» линии шаблонов в гладкие кривые. Он проходит ровно через ключевые точки, но скругляет углы, чтобы персонаж не спотыкался на стыках.
- **Оптимизация рендеринга:** Использование Flutter `Path` и его кэширование в `_cachedPath` позволяет рисовать всю видимую поверхность за один вызов отрисовки (один Draw Call).

### Будущие доработки

- **Object Spawning:** Добавление в `ChunkTemplate` метаданных для спавна монет или препятствий.
- **Memory Management:** Переход к динамической подгрузке: удаление чанков, ушедших за левую границу экрана, и генерация новых впереди.

---

## 🍕 2. Персонаж BuonGiorno (Пепе)

Файл `buon_giorno.dart` управляет логикой главного героя, балансируя между физической стабильностью и визуальной динамикой.

### Физическая конфигурация

- **Тело:** `DynamicBody` с заблокированным вращением (`fixedRotation: true`). Это предотвращает неконтролируемые кувырки персонажа.
- **Движение:** Постоянная горизонтальная скорость `kBonSpeedX` принудительно задается через `linearVelocity`, что создает эффект автоматического раннера.

### Определение наклона (Raycasting)

Для того чтобы персонаж визуально «стоял» на склоне, используется система зондирования:

1.  **Raycast:** Каждые 50 мс выпускается луч из центра тела вниз.
2.  **Анализ нормали:** При попадании в землю извлекается вектор нормали поверхности.
3.  **Slope Angle:** Через $atan2(n.x, -n.y)$ вычисляется угол перпендикуляра к нормали.
4.  **Lerping:** Визуальный компонент плавно поворачивается к целевому углу с помощью линейной интерполяции (`_lerpAngle`), создавая мягкий переход на перегибах холмов.

---

## 🕹 3. Ядро игры: BuonGiornoGame

Основной контроллер, связывающий физику, камеру и компоненты.

### Камера и UX

- **Hill Climb Style:** Камера настроена с использованием `Anchor(0.2, 0.7)`. Это смещает фокус так, что игрок находится в левой нижней части экрана. Это дает максимальный обзор трассы впереди.
- **Zoom:** Значение `0.2 - 0.5` позволяет видеть масштабные горы и заранее готовиться к препятствиям.
- **World Hierarchy:** Все игровые компоненты добавляются в `world` через `world.add()`. Это обеспечивает корректную работу `camera.follow(player)`, так как камера и игрок находятся в одном координатном пространстве.

### Физика мира

- **Гравитация:** Установлена на `20`. Повышенная гравитация делает управление более отзывчивым, а падение — стремительным, что важно для аркадного геймплея с балансировкой веса.

---

## 🛠 Технический стек

- **Движок:** Flame 1.x
- **Физика:** Forge2D (Box2D порт)
- **Язык:** Dart
